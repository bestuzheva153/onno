<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.4</version>
        <relativePath/>
    </parent>

    <groupId>ru.bestuzheva153</groupId>
    <artifactId>onno-subscriptions</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>onno-subscriptions</name>
    <description>Subscription accounting service built on onno</description>

    <properties>
        <java.version>21</java.version>
        <onno.version>2.0.0</onno.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>su.onno</groupId>
            <artifactId>onno-framework-starter</artifactId>
            <version>${onno.version}</version>
        </dependency>
        <dependency>
            <groupId>su.onno</groupId>
            <artifactId>onno-ui-starter</artifactId>
            <version>${onno.version}</version>
        </dependency>
        <dependency>
            <groupId>su.onno</groupId>
            <artifactId>onno-auth-starter</artifactId>
            <version>${onno.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
